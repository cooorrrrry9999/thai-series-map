# Thai Series Map — 改修メモ / ロードマップ

最終更新: 2026-06-13

## 0. このアプリの構成（前提メモ）
- 素のHTML + Leaflet + Firestore + 静的 data.json。Next.jsではない。
- データの流れ: admin.html で編集 → Firestore（正データ）→「📤 公開用 data.json を書き出す」→ data.json → deploy.sh → GitHub Pages。
- 訪問者は data.json を読むだけなので Firestore 読み取りは 0 回（コストを抑える正しい形）。
- 公開URL: https://cooorrrrry9999.github.io/thai-series-map/
- データを直すときは必ず admin.html（=Firestore）側で直す。data.json を手で書き換えない（次の書き出しで上書きされるため）。

## 1. 完了済み（2026-06）
- 作品名の表記ゆれをマスタに統一: `GAP The Series`→`GAP`、`The Secret Of Us`、`My safe zone`→`My Safe Zone`、`4Elenment`(誤字)→解消、`23.5` に統一。
- 作品マスタの同名重複を整理（FayMayNeen / Love Bully / Somewhere Somehow）。
- ダッシュボードに「📤 公開用 data.json を書き出す」「💾 バックアップを保存」ボタンを常設。
- 全作品名がマスタと一致する状態を確認（`プライベート` のみ未マッチ＝区分なので正常）。

## 2. 残っている小タスク（任意・見た目）
- [ ] `23.5` にテーマカラー（グリーン例 `#22C55E`）を設定。
- [ ] `My Safe Zone` の色が未設定（None）→ 色を入れる。
- [ ] 他に色未設定の作品がないか、登録が一段落したら再チェック。

## 3. ユーザー体験（UX）改修の候補
登録が一区切りしたら、実データを見ながら効果の大きい順に着手する。

- [ ] **入力ミス予防**: マスタに無い作品名/CP名が紐付いたら admin.html で警告表示。
      （表記ゆれの再発を防ぐ。入力は自由入力でなく候補から選ぶ運用も徹底）
- [ ] **色塗り忘れ防止**: 色未設定の作品をダッシュボードに一覧表示。
- [ ] **フィルターの使い勝手**: 作品・CPでの絞り込み、件数表示、モバイルでの操作性。
- [ ] **ピン密集対策**: バンコク中心部など密集エリアでクラスタリング（まとめ表示）。
- [ ] （将来）ファン投稿の受け付け: firestore.rules に下書き済み。承認フロー（location_submissions → locations）を有効化するか検討。

## 4. 運用の決めごと（メモ）
- `type` フィールドは現状ほぼ空。使うなら入力必須化、使わないなら廃止を決める。
- `appearances` は作品/CPを「名前」で参照している（ID参照ではない）。将来データが増えて表記ゆれが負担になったら `series_id` / `cp_id` への移行を検討。当面は §3の「警告表示」で運用カバー。

## 5. デプロイ手順（毎回）
1. admin.html でデータを編集。
2. ダッシュボードの「📤 公開用 data.json を書き出す」を押す（必要なら先に「💾 バックアップを保存」）。
3. ダウンロードした data.json を index.html と同じフォルダに置き換える。
4. ターミナルで: `cd ~/Desktop/thai-series-map && bash deploy.sh`
5. 数分後 https://cooorrrrry9999.github.io/thai-series-map/ に反映。
